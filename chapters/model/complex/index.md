- Press `←` and `→` keys to rotate arm1
- Press `↑` and `↓` keys to rotate arm2
- Press `z` and `x` keys to rotate palm
- Press `c` and `v` keys to rotate fingers

If not works, click canvas panel and retry
